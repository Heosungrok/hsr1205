소스트리 꼭 깔것!

commit 하기전에 꼭 마지막으로 코딩한거 다시 확인!

1. reset(discard) 삭제.
-hard : 완전히 다날리기
-mixed : working place 는 그대로 두고 index만 지우기


2. revert(reverse) - 되돌리기
Revert하고싶은작업까지 역순으로 할것. (안그러면 충돌일어남)


3. branch
commit한 것이 해당브랜치에서만 저장이 됨.

4. merge
master브랜치로 checkout하고 
merge 실험 into current branch 누르기.